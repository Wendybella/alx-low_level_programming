#include <stdio.h>
/**
 * main - shows all single digit numbers of base 10 starting from 0
 * print single digits numbers
 * Return: always 0 success
 */
int main()
{
int n;
n=69
while(n<79)
{
putchar(n);
n++;
}
putchar(10);
return (0);
}
