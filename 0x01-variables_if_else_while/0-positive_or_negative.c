#include <stdio.h>
/**
 * main - entry point
 * might be negative or postive
 * Return: always 0 (success)
 */
int main(void) 
{

	int n;
if (n == 0)
printf("zero\n", n);
else if (n < 0)
printf("negative\n", n);
else
printf("positive\n", n);
return (0);
}
