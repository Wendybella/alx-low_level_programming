#include <stdio.h>
/**
 * main - determines all single digit numbers from 0 in base 10
 * single digits are to be printed
 * Return: 0 always
 */
int main(void)
{
int x=28;
while (x < 38)
{
putchar (x);
x++
}
putchar (10);
return (0);
}
