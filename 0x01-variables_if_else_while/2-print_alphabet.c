#include <stdio.h>
/**
 * main - show alphabet in lowercase
 * print alphabet in lowercase
 * Return: 0 normally
 */
int main(void)
{
char a = 97;
while (a < 123)
{
putchar (a);
a++;
}
putchar (10);
return (0);
}
