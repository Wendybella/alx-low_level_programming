#include "main.h"
/**
 * rev_string - reverse string
 * @s: the reverse string to be printed
 */
void rev_string(char *s)
{
int a;
char b;
int c;
int a = 0;
int c = 0;
char b;
while (s[c++])
a++;
}
for (c = a - 1; c >= a / 2; c--)
{
b = s[c];
s[c] = s[a - c - 1];
s[a - c -1]= b
}
}
