#include "main.h"
 /**
  * _isupper.c - print uppercase letter
  * return: 1(if c is uppercase) 0 (otherwise)
  */
int _isupper(int c);
{
int uppercase;
if(uppercase >= 65 && uppercase <= 90)
{
return(1);
}
else
{
return (0);
}
}
